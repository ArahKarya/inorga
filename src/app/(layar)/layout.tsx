/**
 * Kerangka polos untuk layar besar (proyektor di arena): tanpa bilah peran,
 * tanpa gulir halus, tanpa ruang atas. Kontennya harus tampil seketika.
 */
export default function LayoutLayar({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
